import { DateAsLastXPipe } from './date-as-last-x.pipe';

describe('DateAsLastXPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAsLastXPipe();
    expect(pipe).toBeTruthy();
  });
});
